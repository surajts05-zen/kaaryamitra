version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: km_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-kaaryamitra}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-km_secret_change_me}
      POSTGRES_DB: ${POSTGRES_DB:-kaaryamitra_db}
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-kaaryamitra}']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: km_redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: km_api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://${POSTGRES_USER:-kaaryamitra}:${POSTGRES_PASSWORD:-km_secret_change_me}@postgres:5432/${POSTGRES_DB:-kaaryamitra_db}
      REDIS_URL: redis://redis:6379
    ports:
      - '3000:3000'
    env_file:
      - ./apps/api/.env

  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    container_name: km_web
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - '80:80'
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:3000}

volumes:
  postgres_data:
  redis_data:
