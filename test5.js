const fetch = require('node-fetch'); async function test() { const loginRes = await fetch('http://127.0.0.1:3000/api/v1/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'ca@vyomalabs.online', password: 'password123' }) }); const loginData = await loginRes.json(); const token = loginData.data.token; const reqHeaders = { 'Authorization': 'Bearer ' + token, 'x-tenant-id': loginData.data.user.tenantId }; const pendingRes = await fetch('http://127.0.0.1:3000/api/v1/t/' + loginData.data.user.tenantId + '/workflows/pending', { headers: reqHeaders }); const pendingData = await pendingRes.json(); console.log(JSON.stringify(pendingData, null, 2)); } test();
